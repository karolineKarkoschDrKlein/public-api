title: Cooperation
type: object
description: A cooperation represents two partners working together
properties:
  _embedded:
    type: object
    properties:
      partner:
        $ref: ./agent.v1.yaml
      creatingPartner:
        $ref: ./agent.v1.yaml
    required:
      - partner
      - creatingPartner
  id:
    type: string
required:
  - _embedded
x-tags:
  - Cooperations
